<div class="statistics-card">
    <div class="statistics-header">
        <i class="fa fa-chart-bar statistics-icon"></i>
        <h1 class="statistics-title">{{ gT('Kullanıcı İstatistikleri') }}</h1>
    </div>
    {{ include('./subviews/statistics_user/statistics_user_head.twig') }}
    {{ include('./subviews/statistics_user/statistics_user_content.twig') }}
</div>
<style>
.statistics-card {
    background: #fff;
    border-radius: 14px;
    box-shadow: 0 2px 10px 0 rgba(34,41,47,0.06);
    padding: 2rem 1.2rem 1.5rem 1.2rem;
    max-width: 900px;
    width: 100%;
    margin: 2rem auto;
    font-size: 1.08rem;
    color: #222;
}
.statistics-header {
    text-align: center;
    margin-bottom: 1.2rem;
}
.statistics-icon {
    font-size: 2rem;
    color: #295fa6;
    margin-bottom: 0.3rem;
}
.statistics-title {
    font-size: 1.3rem;
    font-weight: 800;
    color: #295fa6;
    margin-bottom: 0.2rem;
}
@media (max-width: 700px) {
    .statistics-card {
        padding: 1rem 0.3rem 1rem 0.3rem;
        font-size: 1em;
    }
    .statistics-title {
        font-size: 1.05rem;
    }
}
</style>
